# El módulo Metrics no posee tablas propias: expone únicamente agregaciones de
# solo lectura sobre entidades de otros módulos (users, courses, enrollments,
# progress, certificates, ratings). Por eso no define modelos ni migraciones.
